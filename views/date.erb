<!DOCTYPE html>
<html>

    <head>
        <h1>Today</h1>
    </head>

    <body>
        <% today = DateTime.now %>
        <p><%= "The date is #{today.strftime("%A, %B %d, %Y")}" %></p>
    </body>

</html>